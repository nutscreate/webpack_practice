import '../css/main.scss';
import './modernizr-custom.js';
import './_fixed-nav.js';
import './_lightbox.js';
import './_loading.js';

/* const targetElement = document.querySelectorAll(".js-animation");

 document.addEventListener("scroll", function() {
  for (let i = 0; i < targetElement.length; i++) {
    const getElementDistance = targetElement[i].
    getBoundingClientRect().top + targetElement[i].clientHeight * 0
    if (window.innerHeight > getElementDistance) {
      targetElement[i].classList.add("js-animation__on");
    }
  }
}) */

/* const image = new Rellax('.p-card__bg-txt', {
  speed: 1,
  center: true,
}) */


/* (function($){
function floatLabel(inputType){
$(inputType).each(function(){
var $this = $(this);
$this.focus(function(){
$this.prev().addClass("active");
});
$this.blur(function(){
if($this.val() === '' || $this.val() === 'blank'){
$this.prev().removeClass();
}
});
});
}
floatLabel(".floatLabel");
})(jQuery);
 */







/* const myFunc = () => {
  let elem = document.getElementById("p-index-kv");
  let wh = window.innerHeight;
  elem.style.height = wh + "px";
};

window.addEventListener("load", myFunc, false);
window.addEventListener("resize", myFunc, false);
 */
